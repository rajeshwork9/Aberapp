// src/types/gifted-charts.d.ts
declare module 'react-native-gifted-charts' {
  export interface BarChartPropsType {
    /** Enables grouped bars when data = [{ label, bars:[{ value, frontColor }] }] */
    showGroupedBars?: boolean;
  }
}
